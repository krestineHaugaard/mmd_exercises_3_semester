"use strict";

// multiple parameters:

// 1.create another function called preseentPret

// 2. that receives three parameters:

// a. firstName
// b. animalType
// c. animalName

// 3. The function should log ´My name is ${firstName}, I have a ${animalType} called ${animalName}` to the console

preseentPret("Krestine", "dog", "Kefir");

function preseentPret(firstName, animalType, animalName) {
  console.log(
    `My name is ${firstName}, i have a ${animalType}, called ${animalName}`
  );
}
