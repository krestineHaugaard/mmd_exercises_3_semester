"use strict";

// Test the function: presentPet

// preseentPret("Krestine", "dog", "Kefir");

// function preseentPret(firstName, animalType, animalName) {
//   console.log(
//     `My name is ${firstName}, i have a ${animalType}, called ${animalName}`
//   );
// }

// What happens if:

// The function is called with variables named the same as the parameters?

// const firstName = "Krestine";
// const animalType = "dog";
// const animalName = "Kefir";

// // preseentPret("Krestine", "dog", "Kefir");

// function preseentPret(firstName, animalType, animalName) {
//   console.log(
//     `My name is ${firstName}, i have a ${animalType}, called ${animalName}`
//   );
// }

// nothing happens

// The function is called with variables name something diffrent?

// const firstN = "Krestine";
// const animalT = "dog";
// const animalN = "Kefir";

// // preseentPret("Krestine", "dog", "Kefir");

// function preseentPret(firstName, animalType, animalName) {
//   console.log(
//     `My name is ${firstName}, i have a ${animalType}, called ${animalName}`
//   );
// }

// nothing happens

// The function is called with string?

const firstName = "Krestine";
const animalType = "dog";
const animalName = "Kefir";

preseentPret(firstName, animalType, animalName);

function preseentPret(firstName, animalType, animalName) {
  console.log(
    `My name is ${firstName}, i have a ${animalType}, called ${animalName}`
  );
}

// The function is called with a mix of strings and variables?

// The function is called with variables, but in a diffrent order?
